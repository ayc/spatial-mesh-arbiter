# 2026-04-05

## Summary

This was the major compiler-doc closure session. The project moved from an open sketch audit and
active closure queue to a fully closed 125-sketch reference set, then the coordination layer was
restructured so live handoff, historical chronology, and durable decisions no longer compete for
the same file.

## Major Milestones

- Reconciled `memory-bank/` drift against the actual repo state.
- Made it explicit in `AGENTS.md` and `memory-bank/README.md` that `memory-bank/` is orientation
  only and not implementation authority.
- Audited the full 125-sketch corpus against the compiler surface and introduced:
  - `COMPILER_COMPATIBILITY_CHECKLIST.md`
  - `COMPILER_GAP_REGISTER.md`
  - `COMPLETION_CHECKLIST.md`
- Closed the major compiler-gap families needed by the sketch corpus, including CC lifecycle,
  reactivation/runtime state, special combat resolution, control topology, maintained cast/channel
  lifecycle, zone/geometry/projectile extensions, containment/topology, respawn override, and
  shield absorb callbacks.
- Closed the entire sketch corpus as designer-facing references. Final state:
  - `125 Complete / 0 Supported / 0 Partial / 0 Blocked`
- Soft-archived the sketch closure and gap files once the queue was exhausted.
- Introduced `memory-bank/work-log/` and `adr/` so live handoff, chronology, and durable decision
  records are now separated.
- Promoted the first durable compiler decisions into ADRs:
  - `ADR-0001` bounded projectile carry roster
  - `ADR-0002` shield absorb -> resource -> offense loop
  - `ADR-0003` category-scoped cleanse
  - `ADR-0004` late-bound actor transit
  - `ADR-0005` combo-field replacement sequencing
  - `ADR-0006` control-projection teardown callbacks
  - `ADR-0007` target-side status consumption for mass detonation

## Resulting Project State

- `docs-game-compiler/ability-sketches/` is no longer an active backlog.
- Remaining high-value docs work moved back to:
  - ARPG spec promotion in `docs/6-spec-drafts/`
  - deferred extraction execution from `docs-core/06-architecture-section-mapping.md`
- Engine implementation remains early Phase 1:
  - `shared-types` exists
  - `cargo test -p shared-types` passes with 8 tests
  - SimFixed wrappers, baseline profile constants, and fixed-point collision geometry remain

## Tracking Changes

- `SESSION_HANDOFF.md` was reduced to a live checkpoint only.
- `DECISIONS.md` remains the working reverse-chronological index.
- `adr/` is now the durable-decision lane for promoted records.

## Notes

- The worktree is still very dirty and reflects a large documentation wave rather than a clean
  checkpoint commit.
- `.obsidian/workspace.json` is modified and appears unrelated to the engine/compiler work.
